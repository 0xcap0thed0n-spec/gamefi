# Portfolio / trading dashboard

Wallet-connected view of:

- Owned Genesis characters
- Traits / rarity (from metadata)
- Basic activity (mints, transfers) once an indexer exists

## v1 goal

Connect wallet → show token IDs + placeholder traits from testnet. No marketplace required for the first useful build.
