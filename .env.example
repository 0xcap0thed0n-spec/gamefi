# Copy to .env and fill locally — never commit real secrets

# Chain RPC / operator (shape depends on chosen chain)
RPC_URL=
DEPLOYER_PRIVATE_KEY=

# Optional indexer / API keys later
# INDEXER_API_KEY=
